using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class move : MonoBehaviour
{
	/*public Camera _c;

	void Update()
	{
		if (Input.touchCount > 0)
		{
			//Vector3 point = new Vector3(_camera.pixelWidth/2, _camera.pixelHeight/2, 0);
			Touch _touch = Input.GetTouch(0);
			if (_touch.position.x < _c.pixelWidth / 2)
			{
				transform.position -= new Vector3(0.2f, 0f, 0f);
			}
			else transform.position += new Vector3(0.2f, 0f, 0f);
			if (_touch.position.y < _c.pixelHeight / 2)
			{
				transform.position -= new Vector3(0f, 0.2f, 0f);
			}
			else transform.position += new Vector3(0f, 0.2f, 0f);
		}

	}*/
}